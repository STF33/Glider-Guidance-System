    function cmp = colormap_mcp
% colormap with 30 colors
% discrete colormap
clear cmp
p=255;
% Purple
cmp(1,:) =[0, 0, 0];
cmp(2,:)=[102,0,102]/p;        % 
cmp(3,:)=[102,51,153]/p;  % 
cmp(4,:)=[153,0,153]/p;  % 
cmp(5,:)=[153,51,204]/p;  % 
cmp(6,:)=[204,153,255]/p;  % 
cmp(7,:)=[255,204,255]/p;  % 
% Blue:
cmp(8,:) =[0,0,153]/p;
cmp(9,:) =[0,0,204]/p;
cmp(10,:) =[0,102,255]/p;      % sky blue
cmp(11,:) =[0,204,255]/p;        % Light blue
cmp(12,:) =[102,255,255]/p;        % Light blue
cmp(13,:) =[204,255,255]/p;        % Light blue
% Green
cmp(14,:) =[0,51,0]/p;     % Darkgreen
cmp(15,:) =[0,102,0]/p;        
cmp(16,:) =[0,153,0]/p;        % 
cmp(17,:)=[0,204,0]/p;        %
cmp(18,:)=[0,255,0]/p;        %
cmp(19,:)=[153,255,102]/p;        %
%Brown-yellow
cmp(20,:)=[112,75,0]/p;  % 
cmp(21,:)=[153,102,0]/p;  % 
cmp(22,:)=[204,153,0]/p;  % 
cmp(23,:)=[255,153,0]/p;  % 
cmp(24,:)=[255,255,0]/p;  % 
cmp(25,:)=[255,255,204]/p;  % 
% Red:
cmp(26,:)=[112,0,50]/p;  % 
cmp(27,:)=[153,0,0]/p;  % 
cmp(28,:)=[204,0,0]/p;  % 
cmp(29,:)=[255,0,0]/p;  % 
cmp(30,:)=[255,151,152]/p;  % 

